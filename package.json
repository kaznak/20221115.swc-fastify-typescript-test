{
  "name": "swc-test",
  "version": "0.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": "^18.12.1"
  },
  "scripts": {
    "build": "yarn clean && yarn build:main",
    "build:main": "swc src -d dist",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.3.16",
    "@types/node": "^18.11.9"
  },
  "dependencies": {
    "@fastify/sensible": "^5.1.1",
    "fastify": "^4.9.2"
  }
}
